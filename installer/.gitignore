# Build artifacts for the installer project (not the addon — this folder sits
# at the repo root, outside Sku/, so it has its own ignore rules).
bin/
obj/
dist/
*.user

# Release secrets (Discord webhook URLs). The real file is NEVER tracked — a
# webhook URL is a password. The *.example.txt template IS tracked as docs.
.secrets/*
!.secrets/*.example.txt
